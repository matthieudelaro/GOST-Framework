#include <QDebug>
#include "IA.h"

const Vector<Graph::Node*>* IA::possibleMove(const Vector<Graph::Node*>& currentStat, int pieceId, int direction, Game *game)
{
    qDebug() << pieceId << direction;
    return NULL;
}

